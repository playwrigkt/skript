kotlin {
    experimental {
        coroutines "enable"
    }
}
dependencies {
    compile project(':api:queue')
    compile project(':api:sql')
    compile project(':api:serialize')
    compile project(':api:http')
}
