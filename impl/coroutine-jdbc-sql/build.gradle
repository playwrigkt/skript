kotlin {
    experimental {
        coroutines 'enable'
    }
}

dependencies {
    compile project(':api:sql')
    compile project(':api:coroutine')
    compile "com.zaxxer:HikariCP:$hikari_version"
}
